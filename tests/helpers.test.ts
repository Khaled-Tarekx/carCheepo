// import { createApp } from 'main';
// import { beforeAll } from 'vitest';

// declare global {
// 	var testApp: ReturnType<typeof createApp>;
// }

// beforeAll(async () => {
// 	if (!global.testApp) {
// 		const app = createApp();
// 		global.testApp = app;
// 	}
// });
